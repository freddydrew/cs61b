/** An Integer tester created by Flik Enterprises. */
public class Flik {
    //public static boolean isSameNumber(Integer a, Integer b) -> used to say this, 128 != 128 for generic objects
    //so I changed it to int in the parameter declaration of the method!
    public static boolean isSameNumber(int a, int b) {
        return a == b;
    }
}
